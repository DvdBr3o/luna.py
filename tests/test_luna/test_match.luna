{ :match, :type, :MatchType } = require "match"

1 .match
    [type Number]: x -> x + 3
    -- 1: x -> x + 1
    a: "nop!"
    [meta]: a

-- (((type Number)[meta]) == MatchType) .and "true" .or "false"
-- MatchType
-- (type Number)